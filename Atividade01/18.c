#include <stdio.h>
#include <stdlib.h>

void Area(float a, float b){
   int result;     
   result = (a * b) / 2;
   printf("a area vale a: %d", Result);
   break;
}

int main(){
    float base, altura;
	printf("Informe os valores da base e altura \n");
	scanf("%f %f",&base, &altura);

   	Area(base,altura);
    
	return 0;

}